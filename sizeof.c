#include<stdio.h>
int main()
{
    int a;
    printf("The size of an integer is %d",sizeof(int));
    printf("\nThe size of an float is %d",sizeof(float));
    printf("\nThe size of an char is %d",sizeof(char));
    printf("\nThe size of an Double is %d",sizeof(double));
    return 0;
}